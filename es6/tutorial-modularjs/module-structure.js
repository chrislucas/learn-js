define(
   'moduleid' // opcional, se deixado em branco teremos um modulo anonimo
   ,['dep1', 'dep2'] // opcional
   ,function(de1, dep2) {
   		var moduleid = {
   			do: function() {

   			}
   		};
   		return moduleid;
   }
 );